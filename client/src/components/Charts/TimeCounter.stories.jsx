import TimeCounter from './TimeCounter';

export default {
  title: 'components/DayCounter',
  component: TimeCounter,
};

const Template = () => <TimeCounter />;

export const Default = Template.bind({});
